{
  "buildCommand": "pnpm --filter poker-app build",
  "outputDirectory": "packages/poker-app/dist",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [{ "source": "/((?!assets/).*)", "destination": "/index.html" }]
}
