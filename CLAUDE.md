# Poker Monorepo — Claude Instructions

## ALWAYS apply the poker-dev skill

For every task in this project, invoke the `poker-dev` skill before doing anything else. No exceptions.

## Push rule

Only push to git when the user explicitly says "push". Never push proactively.

## Testing rule

Always test locally in the dev server before committing. Never commit untested code.
